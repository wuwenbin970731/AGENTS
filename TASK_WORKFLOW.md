# Task Workflow

本文件是持久化任务包的按需工作流。只在创建、重开、拆分、迁移任务或任务结构不清时读取；已有任务的普通恢复只读 `TASK.md`、`active_iteration` 和直接引用。

## 1. 选择记录级别

| 级别 | 适用场景 | 记录方式 |
|---|---|---|
| `light` | 局部、低风险、路径明确且不依赖长期恢复 | 不创建任务包 |
| `tracked` | 多步骤、可能中断、需要保存命令或验证证据 | 精简 `TASK.md` + 一个活动迭代 |
| `rigorous` | 架构、公共接口、数据迁移、远程长任务、昂贵资源、难回滚副作用或重要验收不确定性 | `tracked` 内容 + 编码前详细设计与阻塞确认 |

不要仅因修改多个文件选择 `rigorous`。如果工作量升级，允许从 `light` 升为 `tracked`、从 `tracked` 升为 `rigorous`，并记录原因。

## 2. 任务包结构

```text
tasks/YYYY-MM-DD/YYYY-MM-DD-HHMMSS-<slug>/
├── TASK.md
├── iterations/
│   └── Fxxx-<slug>.md
├── design/                 # 当前详细设计过大时按需创建
├── evidence/               # 大体积验证材料按需创建
└── archive/                # 旧任务迁移时按需创建
```

- 从 `tasks/TASK_TEMPLATE.md` 创建 `TASK.md`，从 `tasks/TASK_ITERATION_TEMPLATE.md` 创建迭代。
- `<slug>` 使用简短 ASCII 小写连字符名称。初始迭代为 `F000`，后续按最大已有编号递增。
- `status` 使用 `pending`、`in_progress`、`blocked`、`awaiting_user`、`completed`；`design_status` 见第 5 节。
- `TASK.md` 是当前权威状态和恢复入口；迭代文档是单轮历史证据。同一时间默认只有一个活动迭代。
- 复制模板后删除无关占位项，不为“不适用”保留大段空表格。
- `task_schema: 3` 只作为新模板版本。已有 schema 2 任务包保持有效，不为版本统一批量重写；重新激活时仅按当前需要增量补充 `mode` 或精简入口。

## 3. 当前状态与历史证据

`TASK.md` 只保存恢复当前工作所需的信息：

- 目标、范围、完成标准和当前状态。
- 当前事实、设计与端到端流程摘要。
- `active_iteration`、迭代索引和关联任务。
- 当前可复用命令、验证结论、排障入口和下一步。

迭代文档保存本轮完整证据：

- 触发原因、复现、事实、约束和假设。
- 本轮设计、计划、实际改动和设计偏差。
- 实际执行命令、验证证据、产物和恢复信息。

当前状态允许随新事实更新；历史证据只追加或通过新记录纠正，不无痕改写。原始日志、大型错误堆栈、数据清单和批量结果存入 `evidence/` 或项目产物目录，Markdown 只保留结论、路径、哈希或必要摘录。

## 4. 创建任务

1. 搜索是否已有相同目标或完成标准的任务。
2. 现场检查相关 Git、代码、配置、测试、日志、输出和外部状态。
3. 创建任务目录、`TASK.md` 和 `iterations/F000-<slug>.md`。
4. `tracked` 任务写明目标、相关入口、目标流程、预计改动、验收和恢复入口。
5. `rigorous` 任务完成第 5 节设计后，才能正式编码。

模板是字段提示，不是真实状态。未确认存在的文件、符号、参数、命令和行为不得写成事实。

## 5. 严格设计与确认

`design_status` 使用：

- `draft`：仍在调查或设计。
- `awaiting_user`：存在阻塞决策。
- `implementation_ready`：设计足以编码且无未解决阻塞项。
- `implemented`：已按真实实现回写，偏差均已关闭或解释。

`rigorous` 设计只覆盖本轮改动和必要上下游，按适用性说明：

- 当前入口、调用链、数据流或状态流。
- 目标运行链和相关文件、符号、接口职责。
- 输入输出、参数来源、默认值、合法范围和不变量。
- 异常、降级、重试、观测、兼容性、迁移、回滚和副作用。
- 候选方案、关键取舍、风险、假设、修改位置和验收方法。

会显著改变目标、范围、架构、公共接口、参数语义、兼容性、外部状态或破坏性操作的事项是阻塞项，必须确认。其他细节记录推荐方案和依据后可以继续。大设计可放入 `design/CURRENT.md` 或项目正式文档，`TASK.md` 保留摘要和链接。

## 6. 实现与检查点

- 按可独立验证的结果推进。开始前明确本轮目标、影响范围和验证方法。
- 在设计就绪、完成有验证结果的里程碑、出现阻塞、长任务启动或结束、中断前和最终交付时更新记录；普通代码编辑不单独触发文档更新。
- 先更新活动迭代的事实和证据，再同步 `TASK.md` 的当前状态、流程、命令、验证、排障和下一步。
- 普通设计偏差记录原因和影响后继续；触及阻塞边界时将状态改为 `awaiting_user` 并暂停相关实现。
- 当前仍需复用的命令写入 `TASK.md`，注明目录、前置条件、参数、安全边界和预期产物；一次性或已失效命令留在对应迭代。

## 7. 继续、重开与拆分

- 原实现的缺陷、遗漏、回归或未改变交付边界的小优化：继续原任务，创建下一个 `Fxxx`，不新建平行任务。
- 重开已完成任务：保留原完成记录，增加状态变化和迭代索引，将 `active_iteration` 指向新文件。
- 实质改变目标、完成标准、架构或系统边界、交付物、独立排期或回滚边界：创建关联任务，并在双方记录关系、继承内容和拆分原因。
- 任务完成时先关闭迭代，再更新当前真实快照和验证结论，将 `active_iteration` 设为 `null`。

## 8. 恢复与完成

恢复时：

1. 读取 `TASK.md`；存在活动迭代时再读取该文件及其直接引用。
2. 只有当前问题需要追溯根因或证据时，才读取旧迭代、`archive/` 或大型证据。
3. 重新检查 Git、代码、测试、日志、输出和外部进程，将现场差异回写。

完成前确认：完成标准满足，当前流程与代码一致，阻塞项关闭，复用命令可执行，验证结论有证据，偏差和剩余风险已说明。最终回复引用任务入口，不重复整份任务记录。

## 9. 旧式单文件迁移

- 已完成且没有重新使用需求的旧 Task 保持原样，不为格式统一批量迁移。
- 仍在进行、准备重开、已经过长或频繁读取的旧 Task，先只读检查并向用户提交单文件迁移提案。
- 确认后将原文完整移入 `<task-dir>/archive/legacy-task.md`，校验内容一致；原路径保留轻量跳转文件。
- 基于迁移时的真实代码和运行状态重建 `TASK.md`。不要求追溯拆分全部历史，新工作从不冲突的下一个 `Fxxx` 开始。
- 迁移不改变 Task ID、目标、状态历史和关联关系；未经授权不顺带修改业务代码、其他任务或 Git 提交。

## 10. 内容预算与安全

- `TASK.md` 建议不超过 200 行，超过 300 行必须拆出历史正文、详细设计或验证材料。
- 单个迭代只记录一轮工作；持续扩大且产生独立完成或回滚边界时拆分关联任务。
- 不记录密码、令牌、密钥、私人地址、内部主机或其他敏感数据。
- 任务包可以纳入版本控制，但未经用户确认提交范围，不自动暂存、提交或推送。
