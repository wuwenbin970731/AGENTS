---
task_schema: 2
task_id: <YYYYMMDD-HHMMSS-slug>
iteration: F000
status: in_progress
design_status: draft
created_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
updated_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
completed_at: null
trigger: initial
supersedes: null
---

# <Fxxx：本轮迭代标题>

> 本文件只记录这一轮工作的设计、执行与证据。任务当前权威状态位于 [`../TASK.md`](../TASK.md)；完成本轮后必须同步更新入口，但不得将本文件正文复制回入口。

## 1. 本轮目标与边界

- 触发原因：<初始任务、缺陷、遗漏、回归或小范围优化>
- 本轮目标：<可独立验收的结果>
- 与原目标关系：<为何仍属于同一 Task；若不属于则拆分关联任务>
- 本轮范围：<文件、模块、数据或环境>
- 明确非目标：<内容>

## 2. 复现、事实与约束

### 问题复现或初始状态

- 前置条件：<环境、配置和输入>
- 操作：`<command or steps>`
- 实际结果：<真实观察>
- 预期结果：<约定行为>

### 已确认代码事实

| 事实 | 文件或证据 | 对本轮影响 |
|---|---|---|
| <事实> | `<path:symbol>` / test / output | <影响> |

### 必须保持的锚点

| ID | 约束 | 动作 | 验证方法 |
|---|---|---|---|
| A001 | <必须保持或扩展的行为> | preserve / extend / replace / uncertain | <方法> |

### 待确认问题与临时假设

| ID | 内容 | 类型 | 当前处理 | 影响 |
|---|---|---|---|---|
| D001 | <问题或假设> | blocking / non_blocking | awaiting_user / confirmed / assumed | <影响> |

## 3. 本轮详细技术设计

详细设计只覆盖本轮改动和必要上下游。不适用的项目明确写“不适用”，不要补造不存在的接口、迁移或异常流程。

### 设计状态

- 当前设计状态：draft / awaiting_user / implementation_ready / implemented
- 阻塞确认项：<D001 或“无”>
- 非阻塞假设：<内容和依据，或“无”>
- 外部详细设计：<`../design/CURRENT.md`、项目正式文档或“无”>

### 当前与目标调用链

```text
当前：<入口 path:symbol> → <处理> → <输出>
目标：<入口 path:symbol> → <新增或调整处理> → <输出>
```

### 文件与符号级设计

| 文件 | 函数、类或接口 | 当前职责 | 目标职责 | 输入 | 输出 | 修改原因 |
|---|---|---|---|---|---|---|
| `<path>` | `<symbol>` | <当前职责> | <目标职责> | <输入> | <输出> | <原因> |

### 接口与参数契约

| 参数或字段 | 类型 | 来源 | 默认值 | 合法范围 | 缺失或非法处理 | 影响 |
|---|---|---|---|---|---|---|
| `<name>` | `<type>` | CLI / config / API / env / data | <默认值> | <范围> | error / fallback | <影响> |

### 数据、状态、异常与兼容性

| 阶段或场景 | 输入或检测点 | 处理 | 输出或用户表现 | 不变量或排障信号 |
|---|---|---|---|---|
| <阶段或异常> | `<path:symbol>` | <处理> | <输出> | <约束、日志或产物> |

- 保持兼容的接口：<内容或“不适用”>
- 受影响的调用方：<内容或“不适用”>
- 配置或数据迁移：<内容或“不适用”>
- 回滚入口：<内容或“不适用”>
- 外部副作用边界：<内容或“不适用”>

### 关键设计决策

| ID | 决策 | 类型 | 候选方案 | 推荐方案 | 影响 | 状态 |
|---|---|---|---|---|---|---|
| D001 | <决策> | blocking / non_blocking | A / B | A | <范围、接口、参数、兼容性或外部行为> | awaiting_user / confirmed / assumed |

阻塞项必须确认后才能进入 `implementation_ready`。非阻塞项可以在披露推荐方案和依据后使用 `assumed` 继续，不得表述为用户已确认。

## 4. 实现计划

| 状态 | 切片 | 对应设计项 | 目标 | 修改位置 | 验证方法 |
|---|---|---|---|---|---|
| pending | 1. <切片> | D001 / 参数契约 / 调用链 | <独立交付能力> | `<path:symbol>` | <测试或检查> |

## 5. 实际改动与设计实现对照

### 实际改动

| 文件与符号 | 实际改动 | 对应设计项 | 调用链中的作用 | 状态 |
|---|---|---|---|---|
| `<path:symbol>` | <修改内容> | D001 / 参数契约 / 调用链 | <入口、处理、输出或验证> | pending |

### 设计实现对照

编码前设计不得因实现变化而无痕改写。普通偏差记录后继续；改变范围、架构、接口、参数语义、兼容性或外部行为的偏差需要暂停并重新确认。没有偏差时明确写“无”。

| 设计项 | 编码前设计 | 实际实现 | 是否偏差 | 原因 | 影响与处理 |
|---|---|---|---|---|---|
| D001 | <原设计> | <实际实现> | yes / no | <原因> | <重新确认或处理结果> |

## 6. 执行、命令与验证证据

### 本轮实际执行命令

只记录本轮确实执行过的命令。可长期复用的当前命令还需要同步到 `../TASK.md`。

| 时间 | 目录 | 命令或操作 | 结果 | 证据或产物 |
|---|---|---|---|---|
| <时间> | `<working-directory>` | `<command>` | pass / fail / blocked | `<path>` / 摘要 |

### 验证结论

| 验证层级 | 结果 | 关键证据 | 未覆盖边界 |
|---|---|---|---|
| local / integration / e2e / data | pass / fail / blocked | <实际观察> | <内容> |

大体积日志、错误堆栈、数据清单和批量结果保存到 `../evidence/` 或项目产物目录；这里只记录结论、路径、哈希或必要摘录。

## 7. 对任务入口的更新

- 当前状态变化：<例如 `in_progress → completed`，或“无”>
- 当前设计摘要变化：<内容或“无”>
- 当前调用链变化：<内容或“无”>
- 当前接口或参数变化：<内容或“无”>
- 当前可复用命令变化：<内容或“无”>
- 当前验证结论变化：<内容或“无”>
- 当前排障地图变化：<内容或“无”>
- 下一活动迭代：<路径或“无”>

## 8. 中断恢复与完成记录

- 最后完成：<最近完成的切片与验证>
- 当前进行：<内容或“无”>
- 实际修改文件：<文件列表>
- 尚未验证：<内容或“无”>
- 当前阻塞：<原因或“无”>
- 解除条件：<条件或“无”>
- 下一步：<恢复后的第一个具体动作；已完成时写“无”>
- 完成时间：<未完成时为 `null`>

恢复本轮时先读 `../TASK.md` 和本文件，再重新检查 Git、代码、测试、日志、输出和外部进程；不要仅凭本文档中的历史状态继续执行。
