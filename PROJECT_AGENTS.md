# Repository Guidelines

本文件只补充当前项目事实和例外，并继承全局 `AGENTS.md`。冲突时以离工作目录更近的明确规则为准。

## 进入项目

- 先确认项目根目录、工作区状态，以及与请求直接相关的入口、调用者、配置、测试和文档。
- 优先复用已有脚本、组件和约定，不根据其他仓库经验猜测本项目行为。
- 只读取完成当前请求需要的上下文；稳定架构知识应维护在项目正式文档，而不是长期堆入 AGENTS 或单次任务记录。

## 任务路由

- 按全局规则选择 `light`、`tracked` 或 `rigorous`。
- `light` 任务不创建持久化任务包。
- `tracked` 和 `rigorous` 任务使用 `tasks/YYYY-MM-DD/YYYY-MM-DD-HHMMSS-<slug>/TASK.md`，单轮记录位于同目录 `iterations/Fxxx-<slug>.md`。
- 创建、重开、拆分、迁移任务或不确定维护方式时，读取 `tasks/TASK_WORKFLOW.md`；使用 `tasks/TASK_TEMPLATE.md` 和 `tasks/TASK_ITERATION_TEMPLATE.md`。普通恢复只读任务入口、活动迭代和直接引用。
- 任务记录可以纳入版本控制，但未经用户确认提交范围，不自动暂存、提交或推送。

## 修改与解释

- 只修改目标所需的最小文件集合，不修改无关格式，不静默改变公共接口、默认行为或兼容性。
- 不新增生产依赖，除非已说明必要性、替代方案和影响。
- 保留用户未提交修改；与目标重叠时先检查差异并谨慎合并。
- 在里程碑解释实际修改、关键调用链、设计理由、输入输出、异常路径、验证结果和第一排障入口；详细证据保存在活动迭代，不在对话中重复全文。

## 本地、远程与数据边界

- 从项目配置和文档确认本地、远程、容器、数据、模型和服务边界，不硬编码机器相关路径。
- 本地优先完成阅读、编辑、静态检查和低成本测试；数据、GPU、模型、远程服务或长任务仅在目标需要且授权允许时执行。
- 远程或长任务保留启动、状态检查、日志、输出、安全停止和恢复方式。
- 不把本地检查描述为远程成功，不把模拟结果描述为真实数据或真实推理结果。

## 验证与 Git

- 优先使用项目已有的测试、构建、Lint、类型检查、基准和验证脚本；根据改动风险选择必要层级。
- 数据或实验任务还要检查输入范围、数量、结构、可解码性、输出完整性和约定指标。
- 完成前检查 Git 状态与差异，确认没有无关文件、临时产物、敏感信息或意外依赖变化。
- 不执行破坏性 Git 操作，不覆盖用户工作区。未明确要求时，不提交、推送或创建 PR/MR。
- 用户要求提交时，只暂存确认范围内的文件，并在提交前检查差异和依赖完整性。

## 项目专属信息

以下内容必须通过当前仓库确认；未知项保持“待确认”，不要补造。

### 主要入口与调用链

- 待项目阅读后补充。

### 构建、测试与运行命令

- 待项目阅读后补充。

### 数据、配置、环境和外部服务边界

- 待项目阅读后补充。

### 必须保持的项目不变量

- 待需求和代码事实确认后补充。
