# Repository Guidelines

本文件补充当前项目的执行规则，并继承全局 `AGENTS.md`。如果本文件与全局规则冲突，以本项目更具体的规则为准。

## 项目优先

- 修改前阅读与任务直接相关的代码、配置、测试、README、设计文档和已有任务记录。
- 先确认项目根目录、工作区状态、构建方式和测试入口，不根据其他仓库经验猜测本项目行为。
- 识别入口、调用者、数据或状态流、依赖、配置和测试，再决定修改位置。
- 优先复用项目已有脚本、组件和约定，不重复实现已有能力。

## 修改范围

- 只修改完成当前目标所需的最小文件集合。
- 不重构无关代码，不修改无关格式，不静默改变公共接口、默认行为或兼容性。
- 不新增生产依赖，除非先说明必要性、替代方案和影响。
- 保留用户已有的未提交修改；如果任务范围与现有修改重叠，先检查差异并谨慎合并。

## 复杂任务记录

复杂任务统一记录在：

```text
tasks/YYYY-MM-DD/YYYY-MM-DD-HHMMSS-<slug>.md
```

规则如下：

- 使用 `tasks/TASK_TEMPLATE.md` 作为模板；模板不存在时，按本文件规定的字段创建。
- `<slug>` 使用简短、可识别的 ASCII 小写连字符名称。
- 状态使用 `pending`、`in_progress`、`blocked`、`awaiting_user`、`completed`。
- 编码前建立任务文档；每个实现切片完成、验证完成、出现阻塞或准备中断时及时更新。
- 任务文档至少包含：目标、非目标、完成标准、代码事实、约束、疑问与假设、详细技术设计、设计确认项、实现计划、实际改动、设计实现对照、可复用运行命令、验证记录、排障地图和恢复入口。
- 详细技术设计只覆盖本次改动和必要上下游，必须基于实际代码并使用 `<path:symbol>` 定位文件、函数、类或接口。未确认存在的符号、配置和行为不得写成代码事实。
- 接口和参数设计需要说明输入输出、类型、来源、默认值、合法范围、缺失或非法处理及影响；按实际项目补充数据或状态不变量、异常观测、兼容性、迁移和回滚设计，不适用时明确标记。
- 复杂任务在 `design_status = implementation_ready` 且没有未解决的阻塞设计项后才开始正式编码。阻塞项必须等待用户确认；非阻塞项披露推荐方案和假设后可以继续。
- 实现产生启动、调用、生成、训练、推理或部署命令时，在任务文档中保存可直接复用的完整命令，并注明工作目录、前置条件、可替换参数和预期产物；命令变化时同步更新。
- 创建新任务前搜索现有任务文档。后续问题仍属于原目标和完成标准，且只是原实现的缺陷、遗漏、回归或小范围优化时，继续原任务，不新建平行文档。
- 重开已完成任务时，将当前状态改回 `in_progress` 并更新 `updated_at`；在状态历史中记录 `completed → in_progress` 的原因，为本轮分配递增的 `F001`、`F002` 等迭代编号。保留原来的完成记录、实际改动、验证证据和执行历史，只追加新证据并更新当前真实流程、命令、排障地图和恢复入口。
- 如果后续工作改变目标或完成标准，扩展到新的架构或系统边界，或需要独立交付、排期、提交或回滚，则创建关联任务。新任务记录 `parent_task` 和 `relationship`，说明拆分原因、继承内容与新增范围；原任务追加对新任务的反向链接。
- 任务再次完成时，在状态历史和对应后续迭代中记录新的完成时间与验证，不把首次完成记录改写成从未完成过。
- 任务文档可以作为项目文档纳入版本控制，但不得在未确认提交范围时自动暂存或提交。
- 任务文档不得包含凭据、私人路径、内部主机地址或其他敏感信息。

## 编码与解释

- 复杂任务先输出当前代码事实地图、目标调用链、文件与符号级设计、接口和参数契约、异常与兼容性设计、修改范围和验收方案，再按纵向切片编码。
- 每个切片完成后，更新任务文档并向用户说明：改了什么、为什么这样改、代码如何流转、参数和异常契约如何变化、如何验证、出错时从哪里定位。
- 每个切片将实际实现与编码前设计对照。普通实现偏差记录后继续；改变范围、架构、接口、参数语义、兼容性或外部行为的偏差需要重新确认。最终交付前将设计更新为代码的真实状态，并关闭或解释所有偏差。
- 当详细设计过大时，可以拆分到 `docs/design/<task-id>.md`；任务文档仍需保留设计摘要、状态、阻塞确认项、链接和设计实现偏差，不能只保留外部链接。
- 对稳定且长期存在的架构知识，更新项目正式文档；不要让单次任务记录成为架构的唯一事实来源。
- 注释优先解释非显而易见的原因、约束和风险，不逐行复述代码。

## 本地与远程边界

- 从项目配置和已有文档中确认本地、远程、容器、数据和模型的实际边界，不硬编码机器相关路径。
- 本地优先完成代码阅读、编辑、静态检查和低成本测试。
- 数据、GPU、模型、远程服务或长时间任务只在任务明确需要且环境允许时执行。
- 远程或长任务需要记录启动方式、日志位置、输出位置、状态检查方法和安全恢复方式。
- 不把本地静态检查描述成远程执行成功，也不把模拟结果描述成真实数据或真实推理结果。

## 验证

- 优先使用项目已有测试、构建、Lint、类型检查、基准和验证脚本。
- 修改后至少运行与改动直接相关的检查；高风险或跨模块改动补充集成或端到端验证。
- 验证数据或实验任务时，同时检查输入范围、数量、结构、可解码性、输出完整性和约定指标。
- 记录实际执行命令、关键结果和未验证边界，不编造成功结果。
- 完成前检查 Git 状态和差异，确认没有无关文件、临时产物、敏感信息或意外依赖变化。

## Git 与交付

- 不执行破坏性 Git 操作，不覆盖用户工作区。
- 未明确要求时，不自动提交、推送或创建 PR/MR。
- 用户要求提交时，只暂存确认范围内的文件，并在提交前检查差异和依赖完整性。
- 最终交付列出实际修改文件、代码流程、验证结果、剩余风险和任务文档位置。

## 项目专属信息

在下方补充本项目特有信息；没有确认的内容不要填写为事实。

### 主要入口

- 待项目阅读后补充。

### 构建与测试命令

- 待项目阅读后补充。

### 关键数据、配置与环境边界

- 待项目阅读后补充。

### 必须保持的项目约束

- 待需求和代码事实确认后补充。
