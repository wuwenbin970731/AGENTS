# Global Working Agreements

本文件定义跨项目通用的协作方式。项目级 `AGENTS.md` 可以补充或覆盖项目特有规则；离当前工作目录更近的指令优先。

## 基本原则

- 先理解真实上下文，再提出方案或修改代码。优先阅读相关代码、配置、测试和文档。
- 优先最小有效改动，不重构无关代码，不静默引入依赖，不覆盖用户已有修改。
- 区分代码事实、外部事实、推断、建议和临时假设；未经验证的推断不得写成已确认事实。
- 交付结果时报告实际修改文件、真实验证结果、已知风险和未完成事项。

## 复杂度判断

满足以下任一条件时，按复杂任务处理：

- 涉及多个文件、模块、服务或系统层级。
- 包含多个相互依赖的实现步骤。
- 业务规则、技术方案或验收口径存在重要不确定性。
- 需要分阶段开发、远程执行、长时间运行、实验验证或人工验收。
- 中断后仅靠聊天记录难以可靠恢复。

其他任务使用轻量流程，避免为简单修改增加不必要的文档和仪式。

## 编码前：先建立共同理解

复杂任务正式编码前，先给出简洁但完整的代码事实地图和目标方案，包括：

1. 任务目标、预期结果和明确非目标。
2. 现有入口、主调用链、数据流或状态流。
3. 相关模块、配置、依赖、测试和外部边界。
4. 候选方案、关键取舍、风险和临时假设。
5. 预计修改范围、目标调用链和验收方法。
6. 常见失败点与计划保留的排障观测信号。

涉及核心技术选型、可能变化的外部事实或不熟悉的 API 时，先检索权威资料或运行最小验证。仓库内部事实优先通过实际代码、配置和测试确认。

如果某个未确认事项会显著改变范围、架构、兼容性或外部状态，等待用户确认；非阻塞问题可以在明确记录假设后继续。

## 编码中：分片实现并同步解释

- 按可独立验证的纵向切片推进，避免一次性完成大量耦合改动。
- 每个切片开始前说明目标、影响范围和验证方法。
- 每个切片完成后说明：
  - 实际修改的文件和函数。
  - 调用链、数据流或状态流发生的变化。
  - 关键实现决策及其原因。
  - 输入、输出、配置、异常路径和重要不变量。
  - 已执行验证、结果和仍未覆盖的风险。
  - 出现问题时的第一定位入口。
- 解释重点是“为什么这样设计”和“运行时如何流转”，不默认逐行复述代码语法。

## 持久化任务记录

- 复杂任务使用项目约定的持久化任务文档；项目无约定时，使用 `tasks/YYYY-MM-DD/YYYY-MM-DD-HHMMSS-<slug>.md`。
- 任务文档记录目标、非目标、事实、约束、假设、计划、实际改动、可复用运行命令、验证、阻塞、排障地图和恢复入口。
- 如果实现完成后存在启动、调用、生成、训练、推理、部署或其他后续执行命令，将可直接复用的命令及其工作目录、前置条件、参数含义和预期产物写入任务文档，不让关键命令只停留在对话中。
- 创建新任务文档前，先查找是否已有同一目标或同一完成标准的相关任务。原实现的缺陷、遗漏、回归和未改变交付边界的小范围优化，默认继续原任务：将状态重新设为 `in_progress`，追加状态历史和后续迭代，不删除或改写原有完成、实现与验证证据。
- 只有目标、完成标准、架构范围、交付物、独立排期或回滚边界发生实质变化时，才创建新的关联任务；新旧任务相互记录关系、继承内容和拆分原因。
- 在以下节点更新任务文档：编码前、每个切片完成后、启动长任务前后、出现阻塞时、当前工作可能中断前、最终交付前。
- 任务文档是恢复索引，不是真实状态的替代品。恢复时重新检查 Git、代码、测试、日志、进程和输出，以现场为准并回写差异。
- 不在任务文档中记录密码、令牌、密钥、私人地址、内部主机信息或其他敏感数据。

## 验证要求

- 验证应覆盖与风险相称的层级：局部测试、模块或集成测试、端到端真实流程、指标或输出检查、Git 改动范围检查。
- 对数据、模型和实验任务，“能够运行”不等于完成；还需核对数据范围、指标、样例输出、曲线、基准或其他约定信号。
- 只报告实际执行过的命令和观察到的结果。无法执行的验证要说明原因、影响和可复现步骤。
- 不以测试通过代替代码流程解释，也不以代码解释代替真实验证。

## 可解释性交付标准

复杂任务的最终交付至少包含：

1. 30 秒整体摘要。
2. 从入口到输出的端到端调用链。
3. 修改文件和核心模块的职责。
4. 关键设计决策、配置和不变量。
5. 实际验证结果及证据边界。
6. 常见故障的排障地图和最小复现方法。
7. 剩余风险、待确认事项和下一步。

功能验收与可解释性验收都满足后，复杂任务才能标记为完成。

## 中断与恢复

中断前更新任务文档中的恢复入口，至少记录：

- 最后完成的切片。
- 当前进行项。
- 实际修改文件。
- 尚未验证内容。
- 阻塞原因与解除条件。
- 下一步动作。
- 恢复时必须重新检查的真实状态。

恢复任务时，先读任务文档，再检查 Git、当前代码、测试、日志、输出和外部进程；不要仅凭历史对话继续编码。

## 子代理

- 不默认启动子代理。仅在用户明确要求，并且任务可拆成相互独立、可分别验证的工作流时使用。
- 子代理负责提供局部证据和结论；主代理负责统一架构、集成验证和端到端解释。
- 默认由主代理维护任务文档，避免多个代理同时写入造成状态冲突。
