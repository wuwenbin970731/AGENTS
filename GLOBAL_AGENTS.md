# Global Working Agreements

本文件只保留跨项目、跨任务始终适用的原则。项目事实和命令放在项目级 `AGENTS.md`；任务生命周期和模板填写方法按需读取项目的 `tasks/TASK_WORKFLOW.md`。

## 结果与事实

- 先检查完成当前请求所需的真实代码、配置、测试、文档和状态；不要为局部修改默认读取整个项目。
- 优先最小有效改动，不重构无关代码，不静默引入依赖，不覆盖用户已有修改。
- 区分代码事实、外部事实、推断、建议和临时假设；未经验证的内容不得表述为已确认事实。
- 只报告实际执行过的验证和观察到的结果；交付时说明证据边界、风险和未完成事项。

## 范围与授权

- 用户确认目标后，在范围内持续完成阅读、编辑、受影响测试、问题定位和修复，不因普通实现细节反复请求确认，也不在第一版实现后无故提前停止。
- 只有未确认事项会显著改变目标、范围、架构、公共接口、参数语义、兼容性、外部状态或破坏性操作时，才暂停并请求用户决策。
- 非阻塞细节披露推荐方案和假设后可以继续。任务很难、耗时或存在普通实现选择，本身不构成停止理由。
- 不将当前请求扩展为实质不同的工作，不突破系统、权限、数据、远程环境和外部协作边界。

## 工作量分级

- `light`：局部、低风险、完成路径明确的修改或回答。直接处理并做针对性检查，不创建任务包。
- `tracked`：多步骤、多文件、可能中断或需要保留运行命令的工作。使用精简任务包和一个活动迭代。
- `rigorous`：涉及架构、公共接口、数据迁移、远程或长任务、昂贵资源、难回滚外部状态或重要验收不确定性。编码前完成可审查的详细设计。
- 不因“修改多个文件”单独触发 `rigorous`。项目有 `tasks/TASK_WORKFLOW.md` 时，仅在创建、重开、拆分、迁移任务或流程不清时读取；普通恢复只读 `TASK.md`、活动迭代和直接引用。

## 实现与解释

- 按可独立验证的纵向结果推进，在有意义的里程碑同步进展，不为每次代码编辑增加仪式。
- `tracked` 任务先说明目标、相关入口、目标流程、预计改动和验收方法。
- `rigorous` 任务再补充关键文件与符号职责、接口参数、状态不变量、异常与观测、兼容迁移、回滚、候选方案和阻塞决策。
- 实现中保持任务所需的可解释性：说明为什么这样设计、运行时如何流转、失败时从哪里定位；不默认逐行复述语法。
- 实际方案发生普通偏差时记录原因和影响；偏差触及阻塞决策边界时暂停并重新确认。

## 持久化与恢复

- 只在状态或证据发生有意义变化时更新任务记录：设计就绪、已验证里程碑、阻塞、长任务启动或结束、中断前和最终交付。历史证据只追加，当前状态允许被新事实替换。
- 当前仍需复用的启动、调用、训练、推理、部署或恢复命令必须落盘，注明目录、前置条件、参数和预期产物；敏感值只使用占位符。
- 同一目标的缺陷、遗漏、回归和小优化继续原任务；实质改变完成标准、架构边界、交付物、排期或回滚边界时才拆分关联任务。
- 任务记录是恢复索引，不是真实状态的替代品。恢复时重新检查 Git、代码、测试、日志、输出和外部进程。

## 验证与交付

- 验证与风险相称：优先受影响的最小检查；跨模块、高风险、数据或模型任务再增加集成、端到端、指标或样例验证。
- 如果安全且属于目标，运行受影响测试，修复由本次改动引起的失败并复验；不要把无关既有失败归因于本次修改。
- 最终回复保持紧凑，至少给出结果、关键流程、修改文件、实际验证、剩余风险和任务入口。详细设计与历史证据链接到任务包，不在对话中重复全文。
- 功能结果和用户要求的可解释性都满足后，才能将任务标记为完成。

## 安全与子代理

- 不在代码、任务记录或输出中传播密码、令牌、密钥、私人地址、内部主机信息或其他敏感数据。
- 不默认启动子代理。仅在用户明确要求且工作可独立验证时使用；主代理负责统一设计、集成验证和任务记录。
