---
id: <YYYYMMDD-HHMMSS-slug>
status: pending
design_status: draft
created_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
updated_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
design_updated_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
parent_task: null
relationship: null
---

# <任务标题>

## 1. 任务摘要

用几句话说明任务目标、当前状态和预期交付物。

## 2. 状态历史与关联任务

根任务的 `parent_task` 和 `relationship` 保持 `null`。从其他任务拆出的关联任务填写项目相对路径和关系类型，例如 `follow_up`；同时在父任务的关联表中添加反向链接。

### 状态历史

状态变化只追加、不覆盖。重开已完成任务时，保留原完成记录，新增 `completed → in_progress` 记录并关联本轮迭代编号。

| 时间 | 状态变化 | 原因 | 关联迭代 |
|---|---|---|---|
| <时间> | `pending → in_progress` | <开始任务的原因> | initial |

### 关联任务

| 任务文档 | 关系 | 拆分或关联原因 | 继承与新增范围 |
|---|---|---|---|
| 无 | - | - | - |

## 3. 完成标准

### 功能验收

- [ ] <必须实现的行为>
- [ ] <必须保持的兼容性或边界>
- [ ] <必须通过的测试、指标或输出检查>

### 可解释性验收

- [ ] 已说明端到端调用链或数据流。
- [ ] 已披露修改文件、核心符号、接口和参数级设计。
- [ ] 已说明关键设计决策、配置、异常路径、兼容性和不变量。
- [ ] 所有阻塞设计项均已确认，最终设计状态为 `implemented`。
- [ ] 最终调用链和参数契约与实际代码一致，设计偏差已解释；无偏差时已标记“无”。
- [ ] 已将后续需要复用的运行命令及执行条件写入本文档；无相关命令时已标记“不适用”。
- [ ] 已提供常见故障的排障入口和最小复现方法。

## 4. 范围与非目标

### 本次处理

- <范围内事项>

### 明确不处理

- <范围外事项>

## 5. 已确认事实与约束

### 代码事实

| 事实 | 文件或证据 | 影响 |
|---|---|---|
| <已确认事实> | `<path:function>` | <对实现的影响> |

### 用户要求

- <用户明确提出的要求>

### 必须保持的锚点

| ID | 约束 | 动作 | 验证方法 |
|---|---|---|---|
| A001 | <必须保持或扩展的行为> | preserve / extend / replace / uncertain | <验证方法> |

## 6. 待确认问题与临时假设

| 问题或假设 | 类型 | 当前处理 | 影响 |
|---|---|---|---|
| <内容> | blocking / non_blocking | awaiting_user / proceed_with_assumption | <可能影响> |

## 7. 当前事实与详细技术设计

详细设计只覆盖本次改动及必要上下游，不要求复制完整项目架构。不适用的设计项明确写“不适用”，不要机械补造不存在的接口、迁移或异常流程。

### 设计状态

- 当前设计状态：draft / awaiting_user / implementation_ready / implemented
- 设计最后更新时间：<时间，与 frontmatter 的 `design_updated_at` 一致>
- 阻塞确认项：<D001；没有则写“无”>
- 非阻塞假设：<内容和依据；没有则写“无”>
- 外部详细设计：<`docs/design/<task-id>.md`；没有则写“无”>

状态含义：

- `draft`：正在调查和设计，信息尚不足以正式编码。
- `awaiting_user`：存在会改变范围、架构、接口、参数语义、兼容性或外部状态的阻塞设计项。
- `implementation_ready`：设计足以编码且没有未解决的阻塞项；非阻塞细节已披露并记录假设。
- `implemented`：设计已按最终真实代码回写，所有设计偏差均已关闭或解释。

### 当前调用链

只记录通过代码、配置、测试或运行证据确认的当前事实，使用 `<path:symbol>` 标记关键入口和调用点。

```text
<入口 path:symbol>
  → <调用 path:symbol>
  → <数据或状态转换>
  → <外部边界>
  → <当前输出>
```

### 目标运行时调用链

```text
<入口 path:symbol> 接收 <输入>
  → <path:symbol> 校验 <参数或字段>
  → <path:symbol> 执行 <核心逻辑>
  → <持久化或外部系统边界>
  → 返回 <输出或状态>
```

### 文件与符号级设计

| 文件 | 函数、类或接口 | 当前职责 | 目标职责 | 输入 | 输出 | 修改原因 |
|---|---|---|---|---|---|---|
| `<path>` | `<symbol>` | <当前职责> | <目标职责> | <输入> | <输出> | <原因> |

### 接口与参数契约

敏感参数只记录名称、来源和使用边界，不记录真实值。

| 参数或字段 | 类型 | 来源 | 默认值 | 合法范围 | 缺失或非法处理 | 影响 |
|---|---|---|---|---|---|---|
| `<name>` | `<type>` | CLI / config / API / env / data | `<default>` | `<range>` | error / fallback | <影响> |

### 数据流、状态流与不变量

| 阶段 | 输入状态 | 处理 | 输出状态 | 必须保持的不变量 |
|---|---|---|---|---|
| <阶段> | <输入> | <处理> | <输出> | <约束> |

### 异常与观测设计

| 场景 | 检测位置 | 处理方式 | 对上游或用户的表现 | 排障信号 |
|---|---|---|---|---|
| <异常> | `<path:symbol>` | fail / retry / fallback | <表现> | <日志、指标或产物> |

### 兼容性、迁移与回滚

- 保持兼容的接口：<内容或“不适用”>
- 受影响的调用方：<内容或“不适用”>
- 配置或数据迁移：<内容或“不适用”>
- 回滚入口：<内容或“不适用”>
- 外部副作用边界：<内容或“不适用”>

## 8. 实现计划

| 状态 | 切片 | 对应设计项 | 目标 | 修改位置 | 验证方法 |
|---|---|---|---|---|---|
| pending | 1. <切片名称> | D001 / 参数契约 / 调用链 | <独立交付能力> | `<path:symbol>` | <检查或测试> |
| pending | 2. <切片名称> | <设计项> | <独立交付能力> | `<path:symbol>` | <检查或测试> |

## 9. 实际改动

| 文件与符号 | 实际改动 | 对应设计项 | 调用链中的作用 | 状态 |
|---|---|---|---|---|
| `<path:symbol>` | <修改内容> | D001 / 参数契约 / 调用链 | <入口、处理、输出或验证> | pending |

### 设计实现对照

编码前设计不因实现变化而无痕改写。普通实现偏差记录后继续；改变范围、架构、接口、参数语义、兼容性或外部行为的偏差需要暂停并重新确认。没有偏差时明确写“无”。

| 设计项 | 编码前设计 | 实际实现 | 是否偏差 | 偏差原因 | 影响与处理 |
|---|---|---|---|---|---|
| D001 | <原设计> | <实际实现> | yes / no | <原因> | <是否需要重新确认及处理结果> |

## 10. 后续迭代

首次执行时写“无”。原任务完成后出现仍属于原目标和完成标准的缺陷、遗漏、回归或小范围优化时，不创建平行任务；将任务状态改回 `in_progress`，在状态历史中记录原因，并按 `F001`、`F002` 递增追加本节。不得删除或改写此前的实现与验证证据。

如果后续工作实质改变目标、完成标准、架构范围、交付物、独立排期或回滚边界，创建关联任务并在“关联任务”中互相链接，不强行写入本节。

### F001：<迭代标题>

- 触发时间：<时间>
- 触发原因：<问题如何发现>
- 与原目标关系：<为何仍属于原任务；若不属于则拆分关联任务>
- 状态：in_progress / blocked / awaiting_user / completed
- 影响范围：<文件、模块、数据或环境>
- 根因：<已确认根因；未确认时明确标记假设>
- 修改方案：<本轮实际方案>
- 影响的设计项：<D001、接口与参数契约、调用链等>
- 设计是否变化：yes / no
- 是否需要重新确认：yes / no
- 设计实现偏差：<内容；没有则写“无”>
- 影响的运行命令：<无，或指向更新后的命令>
- 验证与结果：<实际验证；进行中时写待验证>
- 完成时间：<未完成时留空>

## 11. 运行与复现命令

记录实现完成后仍需使用的启动、调用、生成、训练、推理、部署或其他执行命令。命令应能够脱离聊天记录被再次使用；没有相关命令时写“不适用”。

每组命令注明：

- 用途和适用场景。
- 执行目录。
- 前置条件，包括依赖、环境、配置和必要输入。
- 可替换参数、默认值和安全边界；使用占位符表示凭据，不记录真实敏感信息。
- 预期输出、产物位置或成功判断信号。

### <命令用途>

- 执行目录：`<working-directory>`
- 前置条件：<环境、依赖、配置或输入>
- 参数说明：`<ARG>` = <含义、默认值或替换方式>
- 预期结果：<输出、产物位置或成功信号>

```bash
<copy-paste-ready command>
```

远程或长时间任务按需同时记录启动、状态检查、日志查看、安全停止和恢复命令。实现接口或参数变化时同步更新本节。

本节保存的是可复用操作方式，不代表命令已经执行；实际执行情况只记录在“验证记录”或“执行记录”中。

## 12. 验证记录

只记录实际执行过的验证和真实观察结果。

| 时间 | 验证 | 结果 | 证据与边界 |
|---|---|---|---|
| <时间> | `<command or check>` | pass / fail / blocked | <关键输出及未覆盖范围> |

## 13. 关键设计决策

| ID | 设计决策 | 类型 | 候选方案 | 推荐方案 | 影响 | 状态 |
|---|---|---|---|---|---|---|
| D001 | <决策> | blocking / non_blocking | A / B | A | <范围、接口、参数、兼容性或外部行为影响> | awaiting_user / confirmed / assumed |

- `blocking`：必须得到用户明确确认后才能进入正式编码。
- `non_blocking`：披露推荐方案和依据后可以继续；使用 `assumed` 时必须记录假设依据，不得表述为用户已确认。
- 实现过程中如果阻塞决策或其影响发生变化，重新标记为 `awaiting_user` 并暂停相关实现。

## 14. 排障地图

| 故障表现 | 第一检查点 | 相关文件或函数 | 最小复现或检查方法 |
|---|---|---|---|
| <现象> | <日志、配置、输入或状态> | `<path:function>` | `<command or steps>` |

## 15. 中断恢复入口

- 最后完成：<最近完成的切片与验证>
- 当前进行：<正在进行的工作>
- 实际修改文件：<文件列表>
- 尚未验证：<未完成验证>
- 当前阻塞：<原因；没有则写“无”>
- 解除条件：<需要的输入或环境变化；没有则写“无”>
- 下一步：<恢复后的第一个具体动作>
- 恢复时重新检查：
  - Git 状态与差异。
  - 当前代码和任务文档是否一致。
  - 相关测试、日志和输出文件。
  - 远程进程、锁、PID 或长任务状态。

## 16. 执行记录

按时间追加关键事件，不删除会影响恢复的重要历史。

- `<时间>`：创建任务文档，状态设为 `pending`。
