---
task_schema: 2
id: <YYYYMMDD-HHMMSS-slug>
status: pending
design_status: draft
active_iteration: iterations/F000-initial.md
created_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
updated_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
design_updated_at: <YYYY-MM-DDTHH:MM:SS+TIMEZONE>
parent_task: null
relationship: null
legacy_document: null
---

# <任务标题>

> 本文件是任务当前状态与恢复索引，不是全部历史的汇总。默认只读取本文件、`active_iteration` 和其直接引用；历史正文保存在 `iterations/`，大体积设计与证据按需读取。

## 1. 30 秒摘要

- 目标：<要实现的结果>
- 当前状态：<已完成、正在做、为何阻塞>
- 当前迭代：[`F000`](iterations/F000-initial.md)
- 下一步：<恢复后的第一个具体动作>
- 主要风险：<内容或“无”>

## 2. 完成标准与范围

### 功能验收

- [ ] <必须实现的行为>
- [ ] <必须保持的兼容性或边界>
- [ ] <必须通过的测试、指标或输出检查>

### 可解释性验收

- [ ] 当前端到端调用链、核心模块职责、接口和参数契约与代码一致。
- [ ] 关键设计决策、异常路径、兼容性和不变量已说明。
- [ ] 所有阻塞设计项均已确认，最终设计状态为 `implemented`。
- [ ] 活动迭代已记录设计实现偏差；无偏差时已标记“无”。
- [ ] 当前可复用运行命令及执行条件已落盘；无相关命令时已标记“不适用”。
- [ ] 已提供排障入口和最小复现方法。

### 本次范围

- <范围内事项>

### 明确非目标

- <范围外事项>

## 3. 当前事实与约束摘要

只保留当前仍有效、会影响恢复或实现的事实。调查过程和历史事实放在对应迭代文档。

| 事实或约束 | 当前证据 | 影响 |
|---|---|---|
| <已确认内容> | `<path:symbol>` / test / output | <影响> |

### 必须保持的锚点

| ID | 约束 | 动作 | 当前验证方法 |
|---|---|---|---|
| A001 | <必须保持或扩展的行为> | preserve / extend / replace / uncertain | <方法> |

### 当前待确认项与假设

| ID | 内容 | 类型 | 当前处理 | 影响 |
|---|---|---|---|---|
| D001 | <问题或假设> | blocking / non_blocking | awaiting_user / confirmed / assumed | <影响> |

## 4. 当前设计快照

- 当前设计状态：draft / awaiting_user / implementation_ready / implemented
- 详细设计：<活动迭代中的章节、`design/CURRENT.md`、项目正式文档或“不适用”>
- 设计最后更新时间：<与 frontmatter 的 `design_updated_at` 一致>

### 当前端到端调用链

只记录已由代码、配置、测试或运行证据确认的当前真实流程。

```text
<入口 path:symbol> 接收 <输入>
  → <path:symbol> 校验或转换 <参数或数据>
  → <path:symbol> 执行 <核心逻辑>
  → <外部系统、持久化或产物边界>
  → 返回 <输出或状态>
```

### 当前模块职责

| 文件与符号 | 当前职责 | 输入 | 输出 | 第一排障信号 |
|---|---|---|---|---|
| `<path:symbol>` | <职责> | <输入> | <输出> | <日志、测试或产物> |

### 当前接口与参数契约

只保留当前调用或复现需要知道的参数。敏感参数记录名称和来源，不记录真实值。

| 参数或字段 | 类型与来源 | 默认值或范围 | 非法处理 | 影响 |
|---|---|---|---|---|
| `<name>` | `<type>`；CLI / config / API / env / data | <默认值与合法范围> | error / fallback | <影响> |

### 当前不变量、异常与兼容性

- 数据或状态不变量：<内容或“不适用”>
- 主要异常路径：<检测点、处理和用户表现>
- 兼容或迁移约束：<内容或“不适用”>
- 回滚入口：<内容或“不适用”>
- 外部副作用边界：<内容或“不适用”>

## 5. 状态历史、迭代索引与关联任务

状态变化和索引只追加一行，不把迭代正文复制到本文件。

### 状态历史

| 时间 | 状态变化 | 原因 | 关联迭代 |
|---|---|---|---|
| <时间> | `pending → in_progress` | <原因> | F000 |

### 迭代索引

| 迭代 | 状态 | 摘要 | 文档 | 完成时间 |
|---|---|---|---|---|
| F000 | in_progress | <初始实现摘要> | [`iterations/F000-initial.md`](iterations/F000-initial.md) | - |

### 关联任务

| 任务入口 | 关系 | 拆分或关联原因 | 继承与新增范围 |
|---|---|---|---|
| 无 | - | - | - |

## 6. 当前可复用运行命令

这里只保存实现完成后仍需复用的当前命令。一次性验证命令和已失效命令留在对应迭代文档；没有相关命令时写“不适用”。

### <命令用途>

- 执行目录：`<working-directory>`
- 前置条件：<环境、依赖、配置或输入>
- 参数说明：`<ARG>` = <含义、默认值和安全边界>
- 预期结果：<产物位置或成功信号>

```bash
<copy-paste-ready command>
```

远程或长任务按需保留启动、状态检查、日志查看、安全停止和恢复命令。凭据只使用占位符。

## 7. 当前验证结论

只保留决定当前任务状态所需的结论。详细步骤和证据链接到对应迭代或 `evidence/`。

| 验证层级 | 当前结论 | 最近证据 | 未覆盖边界 |
|---|---|---|---|
| local / integration / e2e / data | pass / fail / blocked | [`F000`](iterations/F000-initial.md) / `<path>` | <内容> |

## 8. 当前排障地图

| 故障表现 | 第一检查点 | 相关文件或函数 | 最小复现或检查方法 |
|---|---|---|---|
| <现象> | <日志、配置、输入或状态> | `<path:symbol>` | `<command or steps>` |

## 9. 中断恢复入口

- 最后完成：<最近完成的切片与验证>
- 当前进行：<正在进行的工作>
- 活动迭代：[`F000`](iterations/F000-initial.md)
- 实际修改文件：<文件列表>
- 尚未验证：<内容或“无”>
- 当前阻塞：<原因或“无”>
- 解除条件：<需要的输入或环境变化，或“无”>
- 下一步：<恢复后的第一个具体动作>
- 恢复时必须重新检查：Git 状态、当前代码、相关测试、日志与输出，以及远程进程或长任务状态。

## 10. 入口维护约束

- `TASK.md` 建议不超过 300 行；超过 400 行时必须检查并拆出历史正文、详细设计或验证明细。
- 不在此处追加完整 Fxxx 正文、原始日志、大型错误堆栈、数据清单或批量结果。
- 当前状态、调用链、命令和排障信息允许随新事实更新；历史证据只追加到对应迭代或证据文件。
- 同一时间默认只有一个活动迭代。任务完成时将 `active_iteration` 设为 `null`；阻塞或等待用户时保留当前路径；重开时创建新的递增迭代并重新设置。
- 关闭一轮迭代时，先完成迭代文档，再更新本文件的当前快照、状态历史和迭代索引；没有后续工作时将 `active_iteration` 设为 `null`。
- 旧式单文件迁移时，原文完整保存在 `archive/legacy-task.md`；本文件必须基于迁移时的真实现场重建。
